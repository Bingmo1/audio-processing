class AudioProcessor:
    def __init__(self, audio_file):
        self.audio_file = audio_file

    def remove_noise(self, threshold):
        print(f"Removing noise from audio {self.audio_file} with threshold {threshold}")

# Example usage
processor = AudioProcessor("example_audio.wav")
processor.remove_noise(0.5)
